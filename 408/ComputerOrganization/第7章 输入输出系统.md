## 1. I/O接口

### 1.1 IO接口的功能

1.   地址译码和设备选择
2.   通信联络与时序协调
3.   数据缓冲
4.   信号格式转换
5.   控制命令和状态信息传递

>   **I/O 接口**：是一个硬件电路芯片/外设板卡（如网卡、显卡、磁盘控制器），包含多个寄存器。
>
>   **I/O 端口**：是指 I/O 接口中能被 CPU 经由系统总线直接访问的**寄存器**（如数据端口、状态端口、控制端口）。

### 1.2 IO端口编址

#### 1.2.1 独立编址

**定义**：I/O 端口拥有独立的地址空间，与主存地址空间完全隔离（例如：主存 0~4GB，I/O 端口 0~64KB）。

**控制信号**：CPU 需要通过专门的 I/O 指令控制线来区分当前访问的是主存还是 I/O 端口。

**特有指令**：只能使用**专门的 I/O 指令**（如 x86 汇编中的 `IN` 和 `OUT` 指令）。

**优缺点**：

-   👍 *优点*：I/O 指令清晰易读；不占用主存地址空间；译码电路简单。
-   👎 *缺点*：I/O 指令类型少、功能弱，程序设计灵活性差；需要额外的控制引脚。

#### 1.2.2 统一编址

**定义**：将 I/O 端口直接看作是主存的一个特殊单元，从主存地址空间中划出一部分分给 I/O 端口使用。

**控制信号**：统一使用主存读写控制信号

**访问指令**：**无需专门的 I/O 指令**，所有**访存指令**（如 `MOV`、`LD`、`ST`）都可以直接用来访问 I/O 端口。

**优缺点**：

-   👍 *优点*：指令系统功能强大，任何针对主存的操作（如位操作、加法等）均可直接作用于端口；程序设计极具灵活性。
-   👎 *缺点*：减少了可用于主存的实际地址容量；由于地址线位数多，端口地址译码电路相对复杂。

## 2. I/O方式

### 2.1 程序查询方式

#### 2.1.1 工作原理

CPU 在启动外设后，通过程序**不断循环读取** I/O 接口的状态寄存器。如果状态为“未准备就绪”，CPU 就原地等待并继续查询，直到“准备就绪”才进行单字/单字节的数据传输。

#### 2.1.2 核心特点

-   CPU 在外设工作期间处于 **“忙等（Busy-waiting）”** 状态，CPU 与外设完全**串行工作**。
-   硬件结构最简单，但 **CPU 利用率极低**。

### 2.2 程序中断方式

#### 2.2.1 工作原理

CPU 启动外设后继续执行自己的主程序。当外设准备就绪时，向 CPU 发出**中断请求信号**。CPU 在**当前指令执行周期结束时**响应中断，暂停当前主程序，转去执行中断服务程序（ISR）完成一次数据传输，完成后再返回原主程序。

#### 2.2.2 核心特点

-   CPU 与外设实现了**部分并行工作**，消除了忙等，大大提高了 CPU 的利用率。
-   适合处理**突发性、随机性**的请求（如键盘输入、鼠标移动、异常处理）。

>   ##### 一、 中断响应阶段（硬件自动完成 —— 隐指令操作）
>
>   1.  **关中断**：CPU 进入中断周期时，硬件自动将中断允许触发器（EIF）清零，拒绝响应新的中断请求，以确保当前断点和基本现场的绝对安全。
>   2.  **保存断点**：硬件自动将当前的程序计数器（PC）和程序状态字（PSW）压入**系统堆栈**。
>   3.  **识别中断源并引出中断服务程序**：由硬件向量产生器产生**中断向量地址**，通过该地址在中断向量表中查到该中断源对应的中断服务程序**入口地址**，送入 PC。
>
>   ##### 二、 中断服务阶段（软件完成 —— 中断服务程序体）
>
>   3.  **保护现场**：在服务程序开头，通过 `PUSH` 指令将通用寄存器的值压入系统堆栈（由程序员编写）。
>   4.  **设置中断屏蔽字（多重中断核心步 🌟）**：
>       -   将当前中断源以及**优先级低于或等于当前中断源**的所有其他中断源的屏蔽位置为 `1`（屏蔽）。
>       -   将**优先级高于当前中断源**的屏蔽位置为 `0`（允许开中断后被其打断）。
>   5.  **开中断（多重中断关键步 🌟）**：执行开中断指令（如 `STI`）。此时，CPU 重新具备响应更高优先级中断的能力。
>   6.  **执行中断服务程序体**：实际进行数据读写、设备处理。*（若在执行期间有更高优先级的请求到来，CPU 将在此阶段再次进入中断响应阶段，形成中断嵌套）*。
>   7.  **关中断（多重中断恢复步 🌟）**：在准备恢复现场前，执行关中断指令（如 `CLI`），确保恢复现场和返回原主程序的过程不被新的中断打断。
>   8.  **恢复现场和中断屏蔽字**：通过 `POP` 指令恢复通用寄存器的原值；恢复原来的中断屏蔽字。
>   9.  **开中断并中断返回**：执行 `IRET` / `RETI` 指令。该指令在硬件上会**自动合并执行两个动作**：从堆栈中弹出原 PC 和 PSW（恢复原主程序断点），同时**重新开中断**，返回原主程序继续执行。

### 2.3 DMA 方式

#### 2.3.1 工作原理

**工作原理**：在外设与主存之间开辟一条**直接的数据传输通路**。当外设需要传输大量数据时，DMA 控制器（DMAC）向 CPU 申请总线控制权（总线请求）。CPU 放弃总线后，由 DMAC 接管系统总线，自动控制数据成块地在外部设备与主存之间高速传输。

#### 2.3.2 核心特点

-   **主存块传输**：适合高速、批量的数据传输（如磁盘读写、网卡大数据包、显存刷新）。
-   CPU 与外设实现了**高度并行工作**。CPU 只有在数据块传输开始前（进行参数初始化）**和**传输结束后（处理善后工作）才需要介入。

#### 2.3.3 DMA 与 CPU 冲突时的三种总线控制方式：

1.  **停止 CPU 访存**：当 DMA 申请总线时，强制 CPU 挂起并放弃总线。传输期间 CPU 停止工作。
2.  **周期挪用（周期窃取）**：当外设准备好一个字时，DMA 挪用**一个或几个存储周期**。如果此时 CPU 也要访存，**DMA 优先**（因为外设数据不及时取走会丢失）；如果 CPU 不访存，则无冲突。
3.  **交替访存（分时复用）**：将一个常规总线周期分为两个子周期，一个给 CPU，一个给 DMA。CPU 和 DMA 各不干扰，不需要总线申请与应答，但对硬件速度要求极高。

| **对比维度**         | **程序中断方式**                              | **DMA 方式**                                 |
| -------------------- | --------------------------------------------- | -------------------------------------------- |
| **数据传输单位**     | 字 / 字节                                     | 数据块                                       |
| **数据流向通路**     | 外设 $\rightarrow$ **CPU** $\rightarrow$ 主存 | 外设 $\rightarrow$ **主存** （不经过 CPU）   |
| **硬件请求主体**     | 外部设备本身                                  | DMA 控制器（DMAC）                           |
| **CPU 响应时机**     | **当前指令执行周期结束时**                    | **当前总线周期结束时**（指令内部亦可）       |
| **是否需要保护现场** | **需要**（通过中断服务程序，开销大）          | **不需要**（CPU 不需要切换程序上下文）       |
| **功能定位**         | 突发事件、随机事件、键盘/鼠标低速外设         | 磁盘、网卡等高速成块数据传输                 |
| **优先级关系**       | 中断请求 优先级低于 DMA 请求                  | DMA 请求 优先级高于 中断请求（防止数据丢失） |
