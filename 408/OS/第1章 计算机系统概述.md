## 1. 操作系统的基本概念

| **段 \ 时间** | **t0**       | **t1**       | **t2**       | **t3**       | **t4**       | **t5**       | **t6**       |
| ------------- | ------------ | ------------ | ------------ | ------------ | ------------ | ------------ | ------------ |
| **$S_1$**     | $\checkmark$ |              |              |              |              |              | $\checkmark$ |
| **$S_2$**     |              | $\checkmark$ |              |              | $\checkmark$ |              |              |
| **$S_3$**     |              |              | $\checkmark$ | $\checkmark$ |              |              |              |
| **$S_4$**     |              |              |              | $\checkmark$ |              |              | $\checkmark$ |
| **$S_5$**     |              |              |              |              | $\checkmark$ | $\checkmark$ |              |

### 1.1 操作系统的特征

- **并发和共享**是最基本的两个特征，两者互为存在条件：
  - 资源共享是以程序的并发为条件的，若系统不允许程序并发执行，则自然不存在资源共享问题
  - 若系统不能对资源共享实施有效的管理，则必将影响到程序的并发执行，甚至根本无法执行
- 如果失去了并发性，则一个时间段内系统中只需运行一道程序，那么就失去了实现虚拟性的意义了。因此，没有并发性，就谈不上虚拟性
- 如果失去了并发性，即系统只能串行地运行各个程序，那么每个程序的执行会一贯到底。只有系统拥有并发性，才有可能导致异步性

#### 并发

- **并发**：两个或多个事件在**同一时间间隔**内发生
- **并行**：系统具有同时进行运算或操作的特性，在**同一时刻**能完成两种或两种以上的工作
  - 可并行的有【处理机与设备】【处理机与通道】【设备与设备】
  - 不可并行的有【进程与进程】
- 引入进程的目的是使程序能够并发执行
- **注意**：
  - 单核 CPU 同一时刻只能执行一个程序，各个程序只能**并发**地执行
  - 多核 CPU 同一时刻可以同时执行多个程序，多个程序可以**并行**地执行

#### 共享

- **共享**：指系统中的资源可供内存中多个并发执行的程序共同使用
  - **互斥共享方式**：
    - 规定一段时间内只允许一个进程访问该资源【称为**临界资源**】
    - A 访问完并释放该资源后，才允许另一进程访问
  - **同时访问方式**：
    - 宏观上，一段时间内由多个进程“同时”访问某类资源
    - 微观上，进程之间可能交替对该资源进行访问

#### 虚拟

- **虚拟**：指将一个物理上的实体变为若干逻辑上的对应物
- 用于实现虚拟的技术称为虚拟技术
  - **时分复用技术**：如虚拟处理器
  - **空分复用技术**：如虚拟存储器

#### 异步

- 进程的**异步性**：由于资源有限，进程的执行不是一贯到底的，而是走走停停的

### 1.3 操作系统的功能和目标

#### 1.3.1 操作系统作为用户与计算机硬件系统之间的接口

1）命令接口

- 用户利用这些操作命令来组织和控制作业的执行
- **联机命令接口**【交互式命令接口】：
  - 适用于分时或实时系统的接口
  - 由一组键盘操作命令组成
  - 用户发送一个命令，系统就执行一次，主要特点是交互性
- **脱机命令接口**【批处理命令接口】：
  - 适用于批处理系统的接口
  - 由一组作业控制命令组成
  - 用户一次性发送命令清单，系统按清单执行，中途不能干预

2）程序接口

- 可以在程序中进行**系统调用**来使用程序接口
- 普通用户不能直接使用程序接口，只能通过**程序代码间接使用**

## 2. 操作系统的发展历程

### 2.1 手工操作阶段

### 2.2 批处理阶段

- 为了解决人机矛盾及 CPU 和 I/O 设备之间速度不匹配的矛盾，出现了批处理系统

#### 2.2.1 单道批处理系统

- 引入**脱机输入/输出技术**（用外围机+磁带完成），并由**监督程序**负责控制作业的输入、输出
- **特点**：自动性、顺序性、单道性
- **优点**：缓解了一定程度的人机速度矛盾，资源利用率有所提升
- **缺点**：
  - 内存中仅能有一道程序运行，只有该程序运行结束之后才能调入下一道程序
  - CPU 有大量的时间是在空闲等待 I/O 完成，资源利用率依然很低

#### 2.2.2 多道批处理系统

- 允许多个用户将若干个作业提交给计算机系统集中处理
- 当某道程序因请求 I/O 操作而暂停运行时，通过中断机制，CPU 转去运行另一道程序
- **特点**：多道、宏观上并行、微观上串行
- **优点**：
  - 资源利用率高，多道计算机共享计算机资源，从而使各种资源得到充分利用
  - 系统吞吐量大，CPU 和其他资源保持“忙碌”状态
- **缺点**：
  - 用户的响应时间较长
  - 不提供人机交互能力

### 2.3 分时操作系统

- 计算机以**时间片**为单位**轮流为各个用户/作业服务**，各个用户可通过终端与计算机进行交互
- **特点**：同时性（多路性）、交互性、独立性、及时性
- **优点**：
  - 用户请求可以被即时响应，**解决了人机交互问题**
  - 允许多个用户同时使用一台计算机，并且用户对计算机的操作相互独立，感受不到别人的存在
- **缺点**：
  - 不能优先处理一些紧急任务
  - 操作系统对各个用户/作业都是完全公平的，循环地为每个用户/作业服务一个时间片，不区分任务的紧急性

### 2.4 实时操作系统

- 为了能在某个时间限制内完成某些紧急任务而不需要时间片排队
- **硬实时系统**：某个动作必须绝对地在规定的时刻（或规定的时间范围）发生【飞机的飞行自动控制系统】
- **软实时系统**：能够接受偶尔违反时间规定且不会引起任何永久性的损害【飞机订票系统、银行管理系统】
- **特点**：及时性、可靠性
- **优点**：能够优先响应一些紧急任务，某些紧急任务不需时间片排队

### 2.5 其他几种操作系统

- **网络操作系统**：是伴随着计算机网络的发展而诞生的，能把网络中各个计算机有机地结合起来，实现数据传送等功能，**实现网络中各种资源的共享（如文件共享）和各台计算机之间的通信**（如：Windows NT 就是一种典型的网络操作系统，网站服务器就可以使用）

- **分布式操作系统**：主要特点是**分布性和并行性**。系统中的各台计算机地位相同，**任何工作都可以分布在这些计算机上，由它们并行、协同完成这个任务**

- **个人计算机操作系统**：如 Windows XP、MacOS，方便个人使用

| 名称           | 特点                                                         | 优点                                 | 缺点                                 |
| -------------- | ------------------------------------------------------------ | ------------------------------------ | ------------------------------------ |
| 多道批处理系统 | （1）有中断机制；（2）宏观上并行；（3）微观上串行     | （1）资源利用率高；（2）系统吞吐量大 | （1）用户响应长；（2）人机交互能力不足 |
| 分时操作系统   | （1）按照时间片轮流使用；（2）独立，很快得到响应         | 解决了人机交互问题，交互性强         | 需要系统对更短的时间做出处理         |
| 实时操作系统   | （1）硬实时，要求在绝对时间内完成；（2）软实时，可以违反一点点 | 及时，可靠                           | 交互性不如分时操作系统               |
| 分布式操作系统 | （1）多台计算机并行，协同完成；（2）分布式                   |                                      |                                      |

## 3. 操作系统的运行环境

### 3.1 处理器运行模式

- CPU 执行两种不同性质的程序：**内核程序和应用程序**
- 操作系统的内核程序是系统的管理者，既可以执行特权指令，也可以执行非特权指令，运行在核心态
- 为了保证系统能安全运行，普通应用程序只能执行非特权指令，运行在用户态

#### 3.1.1 两种指令

##### 3.1.1.1 特权指令

- 指不允许用户直接使用的指令

- 如：对 I/O 设备操作指令、存取特殊寄存器的指令、有关访问程序状态的指令、置中断指令、关中断指令、清内存指令、置时钟指令

- | 类别        | 指令类型            | 原因说明                                                     |
  | ----------- | ------------------- | ------------------------------------------------------------ |
  | 中断类      | 开关中断指令        | 若用户程序可随意中断CPU，可能导致系统资源被无限占用，需由操作系统控制中断优先级与屏蔽机制 |
  | IO类        | 输入输出（I/O）指令 | I/O操作依赖中断通知CPU完成传输，若不受控会影响系统稳定性，必须由内核管理 |
  | PSW寄存器类 | 写PSW寄存器指令     | PSW包含控制信息（如IF位），若用户可随意修改，可能导致低优先级进程抢占资源，破坏系统调度 |
  | 共享资源类  | 写时钟指令          | 时钟是所有进程共享的节拍源，若被修改将影响所有进程的执行节奏，必须受限于内核态 |

##### 3.1.1.2 非特权指令

- 允许用户直接使用的指令

- 不能直接访问系统中的软硬件资源，只限于访问用户的地址空间

- | 类别                 | 指令类型                                | 原因说明                               |
  | -------------------- | --------------------------------------- | -------------------------------------- |
  | 寄存器类（组合逻辑） | 取数指令                                | 仅访问私有寄存器，不影响其他进程       |
  |                      | 算术指令（加减乘除）                    | ALU在执行时为单进程独占，不共享        |
  |                      | 寄存器清零指令                          | 操作私有寄存器，无需特权               |
  |                      | 压栈/弹栈指令                           | 栈寄存器（EBP/ESP）为进程私有          |
  |                      | 跳转指令（修改PC）                      | PC在取指阶段为进程独占，不干扰其他进程 |
  | 主存类               | 存数指令                                | 主存访问在指令周期内为进程独占         |
  | 共享资源类           | 读时钟指令                              | 只读共享资源，不影响其他进程           |
  | 特殊类               | **trap 指令** | 用于进入内核态，执行于用户态，无需特权 |

#### 3.1.2 两种处理器状态

1）核心态【管态、内核态】

- 此时运行的是内核程序，可以执行特权指令
- 只能在核心态运行的指令和程序：
  - 时钟管理相关的指令【置时钟指令】
  - 中断机制相关的指令【时钟中断程序】
  - 原语相关的指令
  - 系统控制的数据结构与处理【进程调度程序】【进程切换】【缺页处理程序】【系统调用命令】

2）用户态【目态】

- 此时运行的是用户程序，只能执行非特权指令
- 在用户态运行的指令和程序/发生的事件：
  - 命令解释程序【属于命令接口，面向用户】
  - 访管/Trap 指令，跳转指令，压栈指令
  - 广义指令 (系统调用) 的调用
  - 外部中断，缺页

3）如何变态

- **内核态 ---> 用户态**：执行一条**特权指令**
- **用户态 ---> 内核态**：由“**中断**”引发，**硬件自动完成**变态过程，触发中断信号意味着操作系统将强行夺回 CPU 的使用权
- **注意**：
  - 需要操作系统介入的地方，都会触发中断信号

#### 3.1.3 操作系统的内核

- **内核**：是计算机上配置的**底层软件**，是操作系统最基本、最核心的部分。实现操作系统内核功能的那些程序就是**内核程序**

1）与硬件关联紧密的模块

- **时钟管理**：实现计时功能
- **中断处理**：负责实现中断机制
- **原语**：
  - 是一种特殊的程序
  - 处于操作系统最底层，是最接近硬件的部分
  - 该程序运行具有**原子性**（运行只能一气呵成，不可中断）
  - 运行时间较短，调用频繁

2）对系统资源进行管理的功能

- **设备管理**：完成设备的请求和释放，以及设备启动等功能
- **进程管理**：完成进程的创建，撤销，阻塞及唤醒等功能
- **存储器管理**：完成内存的分配，回收以及获取作业占用内存区大小及地址等功能

### 3.2 中断和异常

#### 3.2.1 基本概念

##### 3.2.1.1 中断

**中断**主要来自CPU**外部**，与当前执行的指令**无关**，是异步事件。

-   **可屏蔽中断**：可以通过在CPU中设置中断屏蔽位来忽略的中断。例如，网卡、声卡产生的中断。当CPU正在处理非常关键、不允许被打断的任务时，就会屏蔽这些中断。
-   **不可屏蔽中断**：非常紧急的硬件故障，**绝对不能忽略**，必须立即处理。例如，电源故障、内存读写错误等。

##### 3.2.1.2 异常

异常主要来自CPU**内部**，由当前正在执行的指令**直接导致**，是同步事件。

-   **故障**：通常是可以修复的错误。CPU会转到处理程序，修复后重新执行这条出错的指令。例如**缺页异常**：程序访问的内存地址不在物理内存中，操作系统会从硬盘加载所需页面，然后重新执行那条指令，这次就能成功了。
-   **陷阱**：**有意**安排的异常，通常用于实现**系统调用**。程序主动执行一条特殊指令（如`int 0x80`或`syscall`）来触发异常，从而进入操作系统内核，请求内核服务。这是用户程序主动向操作系统请求服务的方式。
-   **中止**：非常严重的、不可恢复的错误硬件错误。通常无法修复，处理程序只能尽力记录错误信息并终止程序。例如硬件校验错误。

#### 3.2.2 中断处理和子程序调用的比较

| 中断处理                                  | 子程序调用                                       |
| ------------------------------------- | ------------------------------------------- |
| 中断处理程序与被中断的当前程序是相互独立的                 | 子程序与主程序是同一程序的两部分，它们属于主从关系                   |
| 中断的产生是随机的                             | 子程序的调用是通过调用指令 (CALL) 引起的，是由程序设计者事先安排的       |
| 中断处理的过程需要有专门的硬件                       | 完全属于软件处理过程                                  |
| 中断处理程序的入口地址可由硬件向量法产生向量地址，再由向量地址找到入口地址 | 子程序的入口地址是由 CALL 指令中的地址码给出的                  |
| 中断隐指令保存 PC 的内容                        | CALL 指令保存 PC 的内容，先将当前 PC 值压入栈，再将 PC 设置为入口地址 |
| 需对同时检测到的多个中断请求进行裁决                    | 无这种操作                                       |

### 3.3 系统调用

#### 3.3.1 基本概念

- 操作系统对应用程序和程序员提供的接口
- 系统调用需要触发陷入指令（Trap）
- OS 通过提供系统调用避免用户程序直接访问外设
- 在用户程序中，凡是与资源有关的操作（存储分配、I/O 传输及管理文件等）都必须通过系统调用的方式向操作系统提出服务请求，由操作系统代为完成，**保证系统的稳定性和安全性**
- 每个系统调用都有唯一的系统调用号

#### 3.3.2 系统调用与库函数的区别

- **库函数**：
  - 是**语言或应用程序的一部分**，可以运行在**用户空间**中
  - 许多库函数都会使用系统调用来实现功能
  - 有的库函数没有使用系统调用
- **系统调用**：
  - 是**操作系统的一部分**，是内核为用户提供的程序接口，运行在**内核空间**
  - 系统调用要完成上下文的切换和状态的转换，因此未使用系统调用的库函数，执行效率较高

#### 3.3.3 系统调用的过程

1. 传参
2. Trap执行系统调用 （发生在用户态）
3. 由操作系统内核程序处理系统调用请求 （发生在内核态）
4. 返回应用程序

## 4. 操作系统结构

## 4. 操作系统结构

### 4.1 宏内核（单内核 / 单体内核）

-   **宏内核定义**：将操作系统的大部分功能（进程管理、存储管理、设备管理、文件系统等）都放入**内核态**中运行，形成一个功能完整、紧密耦合的内核结构
-   在宏内核中，所有核心模块都运行在**同一内核地址空间**，模块之间通过函数调用直接通信

#### 宏内核特点

-   内核功能完整且庞大
    -   进程管理、内存管理、文件系统、设备驱动等都在内核中
-   模块之间紧密耦合
    -   各模块之间直接调用，通信开销小
-   运行效率高
    -   内核内部调用（函数调用级别），无需进程间通信
-   结构相对简单（对执行效率而言）
    -   不需要复杂的消息传递机制

------

#### 宏内核优点

-   执行效率高
    -   内核态内部通信是函数调用，开销极小
-   系统性能好
    -   资源管理集中，调度效率高
-   硬件访问效率高
    -   驱动程序直接运行在内核态

------

#### 宏内核缺点

-   内核规模大
    -   功能多，结构复杂
-   耦合度高
    -   一个模块出错可能影响整个内核
-   稳定性较差
    -   内核崩溃会导致整个系统崩溃（蓝屏/Kernel Panic）
-   维护与扩展困难
    -   修改一个模块可能影响其他模块

------

### 4.2 微内核

-   **微内核定义**：只保留最核心的功能（如进程调度、内存管理、IPC通信），其余服务（文件系统、驱动等）放在用户态以**服务进程**方式运行

#### 微内核特点

-   内核极小
-   基于**客户/服务器（C/S）结构**
-   通过**消息传递（IPC）**通信
-   机制与策略分离

------

#### 微内核优点

-   内核规模小，结构清晰
-   可移植性强
-   安全性高（服务崩溃不影响内核）
-   易于扩展（新增服务进程即可）

------

#### 微内核缺点

-   性能开销大
    -   用户态 ↔ 内核态 ↔ 用户态频繁切换
-   通信成本高
    -   IPC 消息传递比函数调用慢
-   系统设计复杂

------

### 4.3 宏内核 vs 微内核（重点对比）

| 对比项     | 宏内核       | 微内核          |
| ---------- | ------------ | --------------- |
| 内核规模   | 大           | 小              |
| 服务位置   | 全部在内核态 | 仅核心在内核态  |
| 通信方式   | 函数调用     | 消息传递（IPC） |
| 性能       | 高           | 相对较低        |
| 安全性     | 较低         | 较高            |
| 稳定性     | 差（易崩溃） | 好（模块隔离）  |
| 可扩展性   | 差           | 强              |
| 结构复杂度 | 内核复杂     | 用户态服务复杂  |

------

### 4.4 现代混合结构

-   现实中的操作系统大多采用**混合内核设计**
-   典型代表：
    -   Windows（NT 内核）
    -   Linux（宏内核 + 模块化设计）
    -   macOS（XNU：Mach 微内核 + BSD 宏内核融合）

#### 特点

-   结合宏内核的高性能
-   融合微内核的模块化思想
-   实际工程折中方案

## 5. 操作系统引导


### 5.1 引导整体流程

操作系统引导是指计算机从上电开始，逐步加载操作系统内核并最终进入用户态的全过程。其核心路径为：

> **硬件上电 → BIOS（POST）→ MBR（主引导记录）→ Bootloader（GRUB）→ Linux内核（kernel）→ initramfs → init（1号进程）→ 用户态登录**

这一过程体现了控制权从**固件 → 引导程序 → 内核 → 用户态**的逐级交接。

---

### 5.2 BIOS 阶段（基本输入输出系统）

计算机上电后，CPU 自动跳转到 BIOS（固件）执行，BIOS 完成以下工作：

1. **POST（上电自检）**：检测内存、键盘、显示器等关键硬件是否正常
2. **初始化硬件**：设置基本的`中断向量表`和硬件参数
3. **寻找启动设备**：按 CMOS 中设定的顺序（硬盘、U盘、光驱等）查找可启动设备
4. **加载 MBR**：将启动设备的**第 0 号扇区**（主引导记录，共 512 字节）读入内存 `0x7C00` 位置，并跳转执行

>   MBR 目前对应我们的代码就是 boot.asm

📌 **MBR 结构**（512 字节）：

- **前 440 字节**：引导代码（Bootloader 的第一阶段）
- **接下来的 64 字节**：分区表（4 个分区项，每项 16 字节）
- **最后 2 字节**：魔数 `0x55AA`（标识这是有效的引导扇区）

---

### 5.3 Bootloader（引导加载程序）

MBR 中的引导代码（440 字节）空间有限，无法直接加载内核，因此需要**分阶段加载**完整的 Bootloader。

>   MBR 目前对应我们的代码就是 bootloader.asm，放在`0x9000`这个位置

#### GRUB 的典型引导流程：

| 阶段 | 说明 |
|:---|:---|
| **Stage 1** | MBR 中的 440 字节代码，负责加载 Stage 1.5 或 Stage 2 |
| **Stage 1.5** | 位于 MBR 之后的扇区（紧接 MBR 的 62 个扇区），包含文件系统驱动（如 ext2），使 GRUB 能识别 `/boot` 分区 |
| **Stage 2** | 完整的 GRUB 核心程序，位于 `/boot/grub/` 目录，提供菜单界面、内核加载功能 |

#### Bootloader 的核心任务：

1. **加载内核映像**（`vmlinuz`）到内存
2. **加载 initramfs**（初始 RAM 文件系统）到内存
3. **传递内核启动参数**（如 `root=/dev/sda1`）

> 💡 **为什么需要 initramfs？**  
> 内核本身不包含所有存储驱动（如 SATA、SCSI、RAID 驱动），因此需要一个**临时根文件系统**来加载必要的驱动模块，然后才能挂载真正的根文件系统。

---

### 5.4 内核启动阶段

Bootloader 将内核加载到内存后，CPU 跳转到内核入口（`start_kernel()`），系统进入**内核态**（Ring 0）。

#### 内核初始化的关键步骤（按顺序）：

1. **早期初始化**（`start_kernel()`）
   - 设置 CPU 特权级（进入 Ring 0）
   - 初始化中断描述符表（IDT）和全局描述符表（GDT）
   - 初始化内存管理（建立页表、识别物理内存布局）

2. **核心子系统初始化**
   - 调度器初始化（`sched_init()`）
   - 中断系统初始化（`trap_init()`）
   - 时间管理初始化（`time_init()`）
   - 内存管理初始化（`mm_init()`，建立伙伴系统）

3. **设备驱动加载**
   - 探测硬件设备（PCI 总线扫描）
   - 加载内置驱动（非模块）

4. **挂载 initramfs**（临时根文件系统）
   - 内核将 initramfs（cpio 归档）解压到内存中作为临时根 `/`
   - 执行 initramfs 中的 `/init` 脚本

5. **initramfs 中的 `/init` 脚本工作**：
   - 加载真正的存储驱动模块（如 `ext4`、`nvme` 驱动）
   - 挂载真正的根文件系统（`root=` 参数指定的设备）
   - 切换根（`pivot_root` 或 `switch_root`）到真正的根文件系统
   - 执行 `/sbin/init`（或 systemd）—— **这是 1 号进程**

6. **系统继续启动**：
   - 1 号进程启动所有系统服务
   - 最后启动 `getty` 或登录管理器，等待用户登录

---

### 5.5 用户态的第一个进程（1 号进程）

- **传统 SysVinit**：`/sbin/init` → 读取 `/etc/inittab` → 按运行级别启动服务
- **现代 Systemd**：`/lib/systemd/systemd` → 并行启动服务 → 目标（target）替代运行级别

> 💡 1 号进程是所有用户态进程的**祖先**，PID = 1，它负责启动所有其他用户态进程。

---

### 5.6 特权级变化全景图（考研重点）

| 阶段 | CPU 特权级 | 说明 |
|:---|:---|:---|
| 硬件上电 / BIOS | 实模式（无特权级概念） | 固件初始环境 |
| Bootloader（Stage 1/2） | 实模式 → 保护模式 | 加载内核前切换到保护模式 |
| Linux 内核启动 | Ring 0（内核态） | 内核正式接管，完成初始化 |
| initramfs `/init` | Ring 0（内核态） | 仍在内核态运行 |
| `init` / `systemd`（1号进程） | Ring 3（用户态） | 用户态的第一个进程 |
| 用户登录（shell） | Ring 3（用户态） | 常规用户程序 |

> 📌 **关键理解**：内核启动完成后，通过 `iret` 或 `sysret` 指令从内核态**降级**到用户态，执行 1 号进程。

## 6. 虚拟机

-   **第一类虚拟机监控器 (Type 1 Hypervisor)**：也叫“裸机型”虚拟机监控器。它直接安装在物理硬件之上，**不需要依赖任何操作系统**，可以直接访问和管理CPU、内存等硬件资源。它的角色就像一个精简的、专门用于服务虚拟机的操作系统。
    -   典型例子：VMware ESXi、Microsoft Hyper-V、开源的KVM。
-   **第二类虚拟机监控器 (Type 2 Hypervisor)**：也叫“托管型”虚拟机监控器。它是运行在**现有操作系统（即宿主操作系统）之上的一个普通应用程序**。它本身没有硬件权限，所有对硬件的访问都必须通过宿主操作系统来中转。
    -   典型例子：Oracle VM VirtualBox、VMware Workstation。

